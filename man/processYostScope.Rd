% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processYostScope.R
\name{processYostScope}
\alias{processYostScope}
\title{Process a single scope for the Yost Index}
\usage{
processYostScope(
  yost_data_sub,
  shrink_sub = FALSE,
  geo = NULL,
  year = NULL,
  states = NULL,
  acs_vars = NULL,
  varlist = NULL,
  impute_sub,
  rescale_sub,
  nfactors_sub,
  quiet_sub,
  return_format_sub,
  weight_var_sub = NULL
)
}
\arguments{
\item{yost_data_sub}{An `sf` data frame subset for a single scope (e.g., one state).}

\item{shrink_sub}{Logical, whether to apply the shrinkage stabilization method.}

\item{geo}{The geography level string (e.g., `"tract"`, `"block group"`). Required when `shrink_sub = TRUE`.}

\item{year}{The ACS5 survey year. Required when `shrink_sub = TRUE`.}

\item{states}{A vector of state abbreviations. Required when `shrink_sub = TRUE`.}

\item{acs_vars}{The list of ACS variable names from the attribute. Required when `shrink_sub = TRUE`.}

\item{varlist}{A character vector of the 7 Yost variable names. Required when `shrink_sub = TRUE`.}

\item{impute_sub}{Logical, whether to perform spatial imputation.}

\item{rescale_sub}{The rescaling method ("rank" or "standardize").}

\item{nfactors_sub}{The number of factors to extract in `psych::fa`.}

\item{quiet_sub}{Logical, whether to suppress messages and warnings.}

\item{return_format_sub}{The desired return format (minimal or detailed).
(Note: This is handled by the parent `computeYostIndex` function).}

\item{weight_var_sub}{A string. The name of the column to use for weighting.}
}
\value{
A list containing six elements:
* `df_yost`: Data frame with GEOID, Yost, YostQuintile, and annotation (no geometry).
* `df_raw_values`: The raw input data without geometry.
* `df_geometry`: Data frame containing only GEOID and geometry.
* `df_imputed`: The imputed data without geometry.
* `df_rank`: The ranked/standardized variables used for factor analysis.
* `fa_object`: The `psych::fa` object, or `NULL` if the analysis failed.
}
\description{
This is the primary internal workhorse function for `computeYostIndex`. It
processes a single "scope" (e.g., all national data, or data for a
single state).

It handles:
1. Filtering geographies with no population.
2. Optionally imputing missing data.
3. Harmonizing/rescaling the 7 component variables.
4. Running the factor analysis (`psych::fa`) with robust error handling.
5. Calculating the final Yost score and quintiles.
6. Annotating the final data based on data quality/imputation status.
}
