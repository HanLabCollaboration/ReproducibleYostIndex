% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputeMissing.R
\name{imputeMissing}
\alias{imputeMissing}
\title{Impute Missing Values using Spatial Neighbors (Weighted or Unweighted)}
\usage{
imputeMissing(dfGeo, weight_var_sub = "none", quiet = FALSE)
}
\arguments{
\item{dfGeo}{An `sf` data frame. Must contain a `GEOID` column and valid geometries.}

\item{weight_var_sub}{A string. The name of the column to use for weighting
(e.g., "tot_pop"). If set to `"none"` (default), `NULL`, or `""`, the function
performs an unweighted arithmetic mean.}

\item{quiet}{Logical. If `FALSE` (default), prints progress messages to the console.}
}
\value{
A `tibble` (without geometry) containing the original data with missing
  values imputed. Includes two metadata columns: `nvar_imputed` and `nvar_still_missing`.
}
\description{
A highly optimized spatial imputation function that fills missing values using
the mean or weighted mean of contiguous neighbors (Queen contiguity).
}
